# nginx fixes the map hash bucket size at the first `map` block it parses, so
# this has to be read before any of them — hence conf.d and the 00- prefix.
# nxtsmarthome.com.au's redirect map has keys up to ~70 bytes, past the 64-byte
# default.
map_hash_bucket_size 256;
map_hash_max_size 8192;
